_schema-version: 3.3.0
ID: BP_API
description: A simple CAP project.
version: 1.0.0
modules:
- name: BP_API-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: BP_API-auth
  - name: BP_API-db
  - name: BP_API-destination
  - name: BP_API-connectivity
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: BP_API-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: BP_API-db
  parameters:
    buildpack: nodejs_buildpack
- name: BP_API-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: BP_API-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - bpbusinesspartnerrequest.zip
      name: bpbusinesspartnerrequest
      target-path: resources/
- name: bpbusinesspartnerrequest
  type: html5
  path: app/businesspartnerrequest
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: BP_API-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      oauth2-configuration:
        credential-types:
        - binding-secret
        - x509
      tenant-mode: dedicated
      xsappname: BP_API-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: BP_API-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: BP_API-destination
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
    service: destination
    service-plan: lite
- name: BP_API-connectivity
  type: org.cloudfoundry.managed-service
  parameters:
    service: connectivity
    service-plan: lite
- name: BP_API-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: BP_API-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
